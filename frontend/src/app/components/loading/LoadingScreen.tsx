// components/LoadingScreen.tsx
import styles from './LoadingScreen.module.css'; // Create and customize your CSS module as needed

const LoadingScreen = () => {
  return (
    <div className={styles.loadingContainer}>
      <p>Loading...</p>
    </div>
  );
};

export default LoadingScreen;
